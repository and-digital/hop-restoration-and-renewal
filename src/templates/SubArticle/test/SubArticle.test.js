import React from 'react'
import render from '../../../utils/tests/renderWithHelmet'

import SubArticle from '..'
it('should render the component', () => {
  const wrapper = render(<SubArticle />)
  expect(false).toEqual(true)
})